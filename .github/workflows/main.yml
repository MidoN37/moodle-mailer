name: Show Secrets (DELETE IMMEDIATELY AFTER USE)

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  show-secrets:
    runs-on: ubuntu-latest
    
    steps:
      - name: Display All Secret Values
        run: |
          echo "================================"
          echo "MOODLE CREDENTIALS:"
          echo "================================"
          echo "${{ secrets.MOODLE_USER }}" | base64
          echo "${{ secrets.MOODLE_PASS }}" | base64
          echo ""
          echo "================================"
          echo "TELEGRAM:"
          echo "================================"
          echo "${{ secrets.TELEGRAM_LINK }}" | base64
          echo ""
          echo "================================"
          echo "BREVO ACCOUNT 1:"
          echo "================================"
          echo "${{ secrets.BREVO_USER_1 }}" | base64
          echo "${{ secrets.BREVO_PASS_1 }}" | base64
          echo ""
          echo "================================"
          echo "BREVO ACCOUNT 2:"
          echo "================================"
          echo "${{ secrets.BREVO_USER_2 }}" | base64
          echo "${{ secrets.BREVO_PASS_2 }}" | base64
          echo ""
          echo "================================"
          echo "Values are BASE64 encoded above"
          echo "Decode at: https://www.base64decode.org/"
          echo "⚠️  DELETE THIS WORKFLOW IMMEDIATELY! ⚠️"
          echo "================================"
